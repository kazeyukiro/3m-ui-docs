---
id: users-traffic
title: 用户与流量
---

## 用户

用户名、启用、流量上限、到期时间、绑定节点。凭据（UUID / 密码）可自动生成。

列表上通常能批量启用/停用、延期、加流量（以当前版本按钮为准）。

## 流量

面板从 Mihomo 连接信息汇总用量。核心没起来或 API 不通时，流量页可能暂时为空或停更。

耗尽、到期后的行为由用户状态和订阅逻辑决定：常见是不再下发该用户节点或拒绝认证，以实际版本为准。

## 订阅 Token

每个用户有独立订阅 token，等同于密钥。泄露就在用户管理里轮换/作废。
