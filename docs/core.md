---
id: core
title: 核心管理
---

管的是本机 Mihomo 进程：路径、启动、停止、重启、状态。

安装脚本一般会装好 `/usr/local/bin/mihomo`，并在面板配置里写上。路径不对就改配置或重装。

**配置引擎** 应用成功后会重启或热加载核心（视实现而定）；校验失败不会拿坏配置去顶正在跑的那份。

GeoIP / GeoSite 可在系统设置里更新到 Mihomo 数据目录，路由规则用得到。
