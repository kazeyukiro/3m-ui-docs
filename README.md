# 3m-ui 文档

[Docusaurus](https://docusaurus.io/) 站点，正文在 `docs/`。

```bash
npm install
npm run start    # http://localhost:3000
npm run build
```

改文档：编辑 `docs/*.md`，必要时改 `sidebars.js`。页面顶部需要：

```yaml
---
id: page-slug
title: 标题
---
```
